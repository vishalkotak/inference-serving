# Copy to .env (which is gitignored) and fill in. Never commit real secrets.
HF_TOKEN=
VLLM_BASE_URL=http://localhost:8000/v1
# Cloud GPU creds, if renting (L4 / A10 / etc.)
# RUNPOD_API_KEY=
# LAMBDA_API_KEY=
